
// Profile types module

// Profile data interface
export interface ProfileData {
  bio?: string;
  photoUrl?: string;
  socialLinks?: {
    linkedIn: string;
    twitter: string;
    youtube: string;
    anandCircle: string;
  };
}

// Other profile-related types can be added here
